# v65：人工客服导航与会话展示修复

修复人工客服菜单组和同名子页的路由冲突。点击“人工客服”子页或一级菜单现在明确进入聊天页，不再恢复到上一次的“回复反馈”页。其他分组也遵循明确的点击目标。

客服列表与选中的对话自动刷新，列表展示玩家最新提问，并默认打开等待中的会话。等待接入时保留可见的回复框，提示先接入；刷新不重建已选中的输入框，保留未发送草稿。

管理员绑定玩家的会话不再从列表中隐藏，允许查看本人提问与记录，并标明只读。接入和回复自身会话仍被服务端拒绝。投诉会话的超管限制不变。

验证：166项测试通过；构建141个JS模块、10个HTML页面。新增验证覆盖新请求与最新提问可见、本人会话只读、拒绝本人接入。浏览器实际完成“人工客服 → 回复反馈 → 人工客服”的切换，并确认绑定玩家的提问、聊天记录及只读回复框出现。本次不修改数据库结构或生产业务数据。
