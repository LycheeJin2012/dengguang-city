# v74 — Codex 基于 v69 的 UI 布局重构

用户在撤回 MiniMax UI 后，明确要求由 Codex 重新重构UI。保持v69功能、原有文字与配色、按压模块、后端及数据库；没有恢复已取消的回归修复。

新增独立 css/layout.css，所有12个共享入口加载。只处理页面几何布局、间距、字号层级与断点，不重写原交互状态和按压动画。

- 首页图文比例、服务卡片、统计网格；桌面6列/平板3列/手机2列统计。
- 手机页头固定品牌/菜单/语言分区，账号独立一行。
- 后台桌面侧栏、手机可收起菜单；选择后自动收起并显示当前功能。
- 宽屏地图与地点列表并排，窄屏上下排列；后台地图使用二级标题。
- 聊天、事务、通知、酒店、账号、表格、弹窗统一内容宽度与留白；消息仍同页切换。
- 修正Cloudflare无扩展名地址下的导航选中态。

验证：174项测试通过，151 JS / 12 HTML构建通过，diff check。首页/酒店/事务/地图/消息在320、768、1440宽度没有横向页面溢出；账号/考试/酒店老板/知识库在320宽度检查通过；本地手机后台菜单与客服切换、消息页签草稿保留已实际操作。未进行生产业务写入。回滚可独立revert本次UI提交，保留数据库和业务记录。
