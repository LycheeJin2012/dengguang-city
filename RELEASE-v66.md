# v66：移除榜单、合并客服操作、纵向与多设备布局

- 删除榜单导航与页面，移除玩家榜单和公开赛道排行展示。旧榜单地址跳转首页，排行接口返回已移除状态。个人赛道记录、上报/认证功能和业务数据保留，没有删除玩家或成绩数据。
- 灯灯聊天将最近一条回复的评价与转人工、结束等待、刷新及工单快捷操作合并在同一区域。历史回复仍可评价。刷新或收到新回复时移动原操作节点并重新关联，不丢失按钮行为；正在填写的评价不被轮询清空。
- 菜单、标签、按钮与长文字自动换行；小屏聊天改为单栏。全页约束横向溢出，但保留正常纵向滚动与浏览器缩放。
- 后台表格增加实际文本字段标签，小屏转为纵向卡片，桌面维持表格。长邮箱、编号、日志与操作按钮可换行，不靠裁剪隐藏数据。
- 输入字号、触摸按钮、屏幕安全边距、手机横屏和短窗口弹窗布局均调整。聊天气泡不再被固定高度区域压缩。

验证：167项测试通过，构建142个JS模块、9个HTML页面。浏览器实际检查首页、聊天、工单后台在320/390/768/1024/1440宽度下页面宽度与视口一致、无越界内容；手机审计页93条记录及558个字段标签无横向溢出。酒店、玩家主页和通知页在手机与平板宽度下检查正常。弹窗在320/390竖屏及844×390横屏检查正常。

已实际点击本地聊天的合并操作区：回复评价、转人工、结束等待、刷新均可用。仅使用隔离QA数据；没有在生产创建测试记录。宽度模拟基于内置浏览器，未声称已覆盖所有真实手机或浏览器版本。

本次没有数据库结构变更，现有数据与权限保持。网易组件原型不在本次修改范围。
