# Cloudflare Pages 本地开发环境变量 (wrangler pages dev)
# 复制到 .dev.vars 并填入真实值, .dev.vars 已被 .gitignore 拦截不会 commit
#
# 生产环境 (dengguang-city.pages.dev) 请在 CF Dashboard 设置:
#   Workers & Pages → dengguang-city → Settings → Environment variables
#   (不是这里, 这里只给本地 wrangler dev 用)

# 必需: AI 客服 / AI 草稿的 OpenAI 兼容 API key
OPENAI_API_KEY=sk-your-key-here

# 可选: 自定义 API base URL (默认 https://api.minimax.chat/v1)
# 实际 OpenAI: https://api.openai.com/v1
# 兼容服务: 各家厂商不同, 看文档
OPENAI_BASE_URL=https://api.openai.com/v1

# 可选: 模型名 (默认 abab6.5s-chat)
# 实际 OpenAI: gpt-3.5-turbo / gpt-4 / gpt-4o-mini
OPENAI_MODEL=gpt-3.5-turbo
