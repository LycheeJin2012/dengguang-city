# v53 交接

基线 d20aaf9（v52）；本版统一留言与工单、公开授权、举报/管理员编号匹配、AI 派单建议、酒店老板自助平台、业务留痕及承办奖励。

保留 Cloudflare Pages + D1、现有账号和历史数据。数据库增量版本 57，不插入生产测试账号或演示内容。新增 hotel-owner.html；前后端仍在 js/app 与 functions/_core。

用户最新明确：查看和登录不需要留痕。仅业务变更/上传/导出记录，详情见 README v53。公开默认不同意，审核后可公开文字，附件仍私密。管理员投诉仅超管处理并回避本人。

57 测试通过，99 JS 模块与 9 HTML 构建检查通过。本地真实浏览器验证统一提交、投诉编号、酒店管理及记录、人工派单、10 绿宝石办结、审计 UI、390px 无整体溢出。AI 模拟服务测试通过，浏览器 AI 按钮被自动审批拦截，未验证生产 AI 服务配置。硬件通行密钥未在真实设备重新验收。

本地服务器 tests/dev-server.mjs 使用临时 SQLite，不读取 .dev.vars。不要输出任何真实密钥。发布状态以 GitHub 远端和线上文件/API 验证为准，不把推送成功等同于部署成功。
