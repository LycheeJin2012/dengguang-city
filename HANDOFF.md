# v54 交接

基线 ea30e17（v53）；新增提交后自动派单，紧急 wzc、复杂漫画家，其余历史经验与 AI 辅助。数据库版本 58，原站 Pages + D1 保留。详情见 README v54。

自动派单默认开启；账号按管理员名或绑定玩家名唯一匹配，超管可在派单设置指定 ID、暂停、调整每人上限。不存在/重名/超负荷/回避时不强行指定。新单自动处理，已有派单/人工取消不覆盖，历史未派单提供自动补派。

70 测试通过，103 JS / 9 HTML 构建检查通过。本地浏览器已验证提交→系统派给 wzc→时间线说明，全程无需确认。AI 测试使用模拟服务，不对生产真实工单进行写入验收。发布需分别确认 Git 远端、线上静态文件及 API。

用户要求查看和登录不留痕；业务操作留痕。自动派单/失败原因/策略修改明确标记系统或实际管理员。学习为 180 天历史统计辅助（至少 3 样本），不会自行修改权限或训练模型。

本地 tests/dev-server.mjs 不读取 .dev.vars，使用临时 SQLite、纯虚拟账号。不要输出真实密钥。
