# v51 交接

基线 `7ee879c`；用户要求：前后端整体重做，保留原风格，删除深色模式。

已实施：七页统一前端、独立后台功能导航、真实管理 CRUD、模拟题库、统一权限和请求校验、原子业务/工单写入、正确签到扣费、私信通知隔离、默认数据禁止在读取时自动 seed、通行密钥缺失字段和归属/挑战消费检查。

当前验证：27 个测试通过，包含真实 SQLite SQL、回滚和并发；构建校验 77 JS 模块与 8 HTML 页。浏览器实际操作了管理登录、新建赛道、各管理页、手机宽度 390px、市民登录、酒店提交及历史记录读取、模拟题判题。硬件 Touch ID/Face ID、远程生产 D1 数据的逐项验收不等于本地验证。

本地测试数据位于系统临时目录，不进入生产。运行命令和接口结构见 README。不要参考旧 v50 文档中的“全通过”“已实现”历史结论；它们与真实源码不完全一致。

线上兼容补充：历史图库采用 label/file_url/is_published 列；数据库版本 52 添加别名映射并同步双写，保留原有分类、精选标记和草稿状态。新增真实旧表结构回归用例。
