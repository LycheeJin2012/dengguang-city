# v64：灯灯生成式资料答复、聊天内转人工与结束等待

修正原来“转人工 = 转工单”的误解。灯灯无可靠答案时，现在建立同一私信会话的人工聊天队列，不自动创建工单。管理员在“人工客服”中接入并直接回复原会话；保留署名、回避权限和内部记录。聊天刷新不清空玩家正在输入的消息。

只有人工客服明确点击“建议玩家提交工单”，玩家才会看到工单快捷入口。入口带入客服整理的问题说明，玩家核对后确认提交。建议操作本身不生成工单，不能把其他玩家的工单关联到自己的会话。旧版已经创建的客服工单和其处理历史保留。

等待阶段显示“结束等待”，已接入阶段显示“结束人工会话”。玩家结束后保留聊天记录，暂停自动转人工，避免立即被重新排队；仍可继续向灯灯提问或手动重新转人工。

资料回答不再直接拼接整段原文：服务端检索有效的已审核公开知识和真实公开酒店资料，模型根据当前问题与本会话上下文组织简洁答复，附有效来源。资料不足、模型明确不能回答、模型调用失败或引用无效时转人工。没有可用模型时不冒充生成结果。投诉类会话限制给超管，管理员不能处理自己的绑定玩家会话。

Schema 64 新增 support_chats 和 support_chat_events，转接队列与 tickets 完全分离。没有迁移或删除真实工单数据。

验证：165项测试通过，构建141个JS模块、10个HTML页面。覆盖自动转接无工单、并发去重、结束等待且不自动重排、真人回复同会话、抢占接入、权限回避、客服建议后玩家确认工单、生成式资料答复与引用校验、服务失败转人工。模型答复路径用模拟响应验证，未调用生产模型或创建生产测试会话。

本地浏览器使用原隔离 QA 账号发送“本地验收：树屋酒店在哪里”，确认出现聊天内的等待人工状态和“结束等待”。结束后恢复灯灯模式且保留历史；数据库工单数前后均为1，未新增工单。
