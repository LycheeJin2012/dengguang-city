# v76 — 按首页批注调整与灯灯浮窗

- 主导航回到顶部；六项城市统计各自独立成格。
- 首页工单默认仅取一条摘要：优先本人最新 open/in_progress 工单，否则最新公开工单。进入公开/个人子页签后读取完整列表，支持继续分页与收起。只读API增加校验后的offset及has_more，按时间和数值ID稳定排序，权限不变。
- 城市风貌改为单图轮播，含上/下一张、播放/暂停与图片详情。自动轮换只在可见区域进行，尊重减少动态效果，手动切换后暂停。
- 酒店整个区块（含查看全部房型）在赛道区块之前。
- 全站右下角灯灯浮窗，复用同一聊天、个人资料查询、反馈和人工服务控制器。使用隔离DOM避免与完整消息页ID冲突；关闭保留草稿并暂停隐藏窗口的自动读取；切换账号重新创建视图。不使用iframe，不改安全响应头，不自动退出管理身份。
- 修正结构化工单正文对继承对象键的误处理，防止列表展示异常。

验证：182项测试通过，160 JS / 12 HTML构建通过，diff check。实际验证一条摘要/两条完整列表、轮播切图、浮窗开关草稿、管理员会话保持、输入区可见、320/526/1440浮窗边界、酒店顺序和数据格边框。工单分页及私密记录隔离有独立测试。没有生产测试数据写入或数据库迁移。
