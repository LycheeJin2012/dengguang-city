name = "dengguang-city"
compatibility_date = "2026-08-16"
pages_build_output_dir = "."

# D1 binding - 在 Cloudflare Dashboard → Pages → Settings → Functions → D1 database bindings
# Variable name: DB → dengguang-city-db
# (Git 集成后需要在 Dashboard 手动绑定一次；wrangler.toml 不会生效在 Pages)
