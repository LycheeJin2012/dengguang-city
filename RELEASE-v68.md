# v68 — 消息中心真正同页切换

- 通知与私信改为同一个消息页里的 tab / tabpanel 按钮，不再跳转加载另一文档。
- 页签切换复用面板 DOM，保留当前私信会话、草稿、反馈编辑和通知筛选；支持方向键、Home/End。
- 仅当前页签可见；隐藏聊天暂停轮询读取及自动已读，通知提供刷新按钮。
- 原 notifications.html / dm.html 入口进入相应页签并归一到 messages.html 地址，保留收件人参数。
- 同步 localhost:8884 的旧预览源代码，备份并保留现有 SQLite 测试数据，重启后使用 schema 67。

验证：174 项测试通过，151 JS / 12 HTML 构建通过，diff check。直接在用户的 localhost:8884 标签页验证双向切换、选中态、单一可见面板、未发送草稿保留；验收草稿已清空且未发送。
