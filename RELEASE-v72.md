# v72 — 发布新版按压动画模块

来源：用户再次要求推送新版；与v71逐文件比较后，确认最新运行时代码位于8884工作目录的 js/app/press-motion.js，修改时间2026-09-11 21:37。整站candidate其余运行时文件与线上v71一致。

仅接受该动画模块重构：整理动画状态清理和异常隔离，合并pointerup/pointercancel处理；保持2px下沉、0.974缩放、70ms按下/240ms释放、连按连续性、多指针隔离、禁用控件与减少动态支持。不涉及业务请求、存储、数据库或文字。

验证：174项原正式测试全过，包括按压动画回归；151 JS / 12 HTML构建通过，git diff --check通过。不使用旧预览目录中落后的测试版本替换正式测试。

回滚基线v71 6a3c4d9，发布前完整源码已归档，可独立撤回本次前端提交，不恢复数据库。
