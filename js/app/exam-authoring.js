import {$,$$,api,post,field,modal,tr,esc,text,action,toast,region} from './core.js';
export async function openExamAuthoring(onSaved){
 const data=await api('/api/admin/exam-ai');
 const dialog=modal('AI 驾照出题 · 先审核再入库',`<div class="wide notice">只使用已审核的灯光市考试资料。选择依据后生成 1–5 题；题干、答案与解析都需人工核对。不会自动加入玩家题库。</div>`+field('grade','等级','select','B',{options:['B','A','S']})+field('q_type','题型','select','choice',{options:[['choice','单选'],['multi','多选'],['judge','判断']]})+field('count','数量','number',3,{min:1,max:5})+field('focus','考点 / 难度 / 出题要求','textarea','',{required:false,maxlength:1000})+`<fieldset class="wide"><legend>出题依据（最多选 8 条）</legend>${data.knowledge.map(k=>`<label class="field check"><input type="checkbox" name="knowledge_id" value="${k.id}">#${k.id} ${esc(k.title)} · v${k.revision}</label>`).join('')||'<p>没有可用的考试资料。请让超管到“知识库”导入驾照规则或现有题目，审核为“考试资料”后再出题。</p>'}</fieldset><div class="wide actions"><button type="button" id="generate-exam">生成待审题目</button></div><div class="wide" id="exam-drafts"></div>`,{wide:true});
 const draw=drafts=>{$('#exam-drafts',dialog).innerHTML=drafts.map(d=>`<article class="panel"><h3>待审草稿 #${d.id} · ${esc(d.payload.grade)}</h3><p>${text(d.payload.question)}</p><p>${text(d.payload.options.map((x,i)=>String.fromCharCode(65+i)+'. '+x).join('\n'))}</p><p>建议答案：${esc(d.payload.answer)}</p><p>${text(d.payload.explanation)}</p><small>依据：${d.sources.map(s=>'知识 #'+s.id+' v'+s.revision+' '+esc(s.title)).join('；')}</small><div class="actions"><button type="button" data-exam-review="${d.id}">逐题审核 / 编辑 / 入库</button></div></article>`).join('')||'<p>尚无待审草稿。</p>';$$('[data-exam-review]',dialog).forEach(b=>b.onclick=()=>review(drafts.find(d=>d.id===+b.dataset.examReview)));};
 function review(d){const q=d.payload;modal('审核 AI 驾照题目',field('grade','等级','select',q.grade,{options:['B','A','S']})+field('q_type','题型','select',q.q_type,{options:[['choice','单选'],['multi','多选'],['judge','判断']]})+field('question','题干','textarea',q.question)+field('options','选项（每行一项，判断题留空）','textarea',q.options.join('\n'),{required:false})+field('answer','正确答案（A / A|B / true / false）','text',q.answer)+field('explanation','解析与规则依据','textarea',q.explanation)+field('review','已核对题目、答案、解析与适用规则','checkbox',false),{wide:true,label:'确认审核并加入题库',submit:async v=>{if(!v.review)throw new Error('请先勾选审核确认');await post('/api/admin/exam-ai',{action:'save',draft_id:d.id,confirm_review:true,question:{...v,options:v.options.split('\n').map(s=>s.trim()).filter(Boolean)}});toast('题目已入库并留痕');await onSaved();}});}
 $('#generate-exam',dialog).onclick=e=>action(e.currentTarget,async()=>{const ids=$$('input[name=knowledge_id]:checked',dialog).map(x=>Number(x.value));const d=await post('/api/admin/exam-ai',{grade:$('[name=grade]',dialog).value,q_type:$('[name=q_type]',dialog).value,count:Number($('[name=count]',dialog).value),focus:$('[name=focus]',dialog).value,knowledge_ids:ids});draw(d.drafts);toast(d.note);});draw(data.drafts);
}
