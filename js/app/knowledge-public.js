import {$,api,field,action,esc,text,tr,region} from './core.js';
export async function render(el){el.innerHTML='<h1>灯光市公开知识库</h1><p>仅展示已审核且来源仍有效的资料；资料未覆盖的问题请联系灯灯转人工。</p><div class="toolbar">'+field('q','想了解什么？','text','',{required:false,maxlength:500})+'<button id="knowledge-search">搜索知识</button></div><div id="knowledge-results"></div><a class="button" href="/dm.html">联系灯灯 / 转人工</a>';const load=query=>region($('#knowledge-results',el),()=>api('/api/knowledge?'+query),(d,box)=>box.innerHTML=d.articles.map(a=>`<article class="panel"><h2>#${a.id} ${esc(a.title)}</h2><p>${text(a.question)}</p><p>${text(a.answer)}</p><small>已审核资料 · v${a.revision}</small></article>`).join('')||'<p class="notice">没有找到已审核资料，请联系人工核实。</p>');$('#knowledge-search',el).onclick=e=>action(e.currentTarget,()=>load('q='+encodeURIComponent($('[name=q]',el).value)));const id=new URLSearchParams(location.search).get('id');if(id)await load('id='+encodeURIComponent(id));}
