# v75 — 共用组件与页面工作区重构

按用户要求进行结构性前端重构，不再在旧布局上追加覆盖层。

新增六个 `js/ui` 组件模块；表单、弹窗、页面标题、记录卡片、表格、页签统一实现。重建主导航工作区、首页、档案、事务和酒店的页面组织，后台与审计共用表格，个人/消息/后台共用页签控制器。

旧 `css/layout.css` 删除；旧整块样式替换为五层样式源码与编译产物。保留经过验证的控件/业务视觉和按压模块，移除旧导航/页面布局规则与重复实现。无新依赖，无后端与数据库改动。

验证：178项测试通过（含新组件转义、字段约束、页签键盘及样式编译一致性测试）；157 JS / 12 HTML构建；diff check。实际操作登录弹窗、个人页签、消息草稿、后台/审计表格和后台子页签；首页/酒店/事务/地图/消息在320、768、1440宽度没有溢出；320宽审计表312个数据单元无内部横向溢出；844×390横屏弹窗在视口内。未测试真实手机硬件与生产写入。

完整结构说明见 UI-ARCHITECTURE.md。发布前基线已归档，撤回此UI专用提交可恢复v74，不涉及业务数据库回滚。
