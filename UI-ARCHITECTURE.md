# 前端结构（v75）

## 组件与页面分离

- `js/ui/html.js`：统一数据转义。
- `js/ui/form-field.js`：所有业务表单共用的字段渲染。
- `js/ui/dialog.js`：弹窗生命周期、提交状态、关闭与焦点恢复。
- `js/ui/workspace.js`：应用工作区、页面/分区标题、页签与键盘控制。
- `js/ui/card.js`：公告、酒店等记录卡片；数据标题统一转义。
- `js/ui/table.js`：后台与审计记录共用表格外壳、移动端字段标签。

`js/app` 保留页面的数据加载、业务接口调用和页面适配器。`core.js` 的已有导出继续兼容调用方，字段/弹窗/标题实现委托给组件，而非重复实现。

## 页面结构

- 主框架：页头、独立主导航栏、主内容、页脚。手机导航展开为两列入口。
- 首页：欢迎区；公告与工单主区/城市数据辅助区；风光；报名与酒店服务区。
- 玩家档案：身份摘要与操作栏/个人任务工作区。
- 我的事务：灯灯摘要栏/分类与事务记录区。
- 酒店：筛选栏/结果卡片区。
- 消息：共用页签控制器+独立会话列表与聊天区域，切页签保留现有DOM草稿。
- 后台：管理导航/任务区；共用页签和表格组件，手机菜单可收起。

## 样式来源

`css/source/foundation.css` 是设计变量、文字、原生控件基础。
`components.css` 管理共用组件，`workspaces.css` 管理页面结构和断点。
`business.css` 保存附件、工单、考试等业务专用视觉。
`motion.css` 保存已验证的按压与减少动态效果约束。

运行 `node --experimental-vm-modules scripts/build.mjs` 编译为 `css/style.css` 和兼容URL `style.min.css`。不要直接修改生成文件。原 `css/layout.css` 及其HTML引用已删除。无新框架或依赖。

所有后端API、权限、数据库、业务规则保持不变。样式与组件改动可以独立回滚。
